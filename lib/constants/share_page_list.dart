class SharePageDetails {
  late String pageTitle;
  // final GlobalKey<NavigatorState>? navigatorkey;
  // final String title;
  // final IconData icon;
  late bool isSelected;

  SharePageDetails({required this.pageTitle, required this.isSelected});
}
